<div class="container">
  <h1>Add child to your profile</h1>
  <%= simple_form_for @kid do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :age %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: 'btn btn-primary' %>
  <% end %>
</div>
