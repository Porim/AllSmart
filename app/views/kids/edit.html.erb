<div class="login-container d-flex justify-content-center mt-5">
  <div class="container d-flex justify-content-center mt-5">
    <div>
      <h4 class="mb-4">Edit
        <%= @kid.first_name.possessive %> profile </h4>
      <%= simple_form_for [@kid] do |f| %>
      <%= f.input :first_name, input_html: { class: "name"} %>
      <%= f.input :last_name, input_html: { class: "name"} %>
      <%= f.input :age %>
      <%= f.input :photo, as: :file %>
      <div class="d-flex justify-content-align mb-5">
        <%= f.submit class: "kids-update d-flex justify-content-center align-items-center" %>
        <div>
          <%= link_to 'Back to profile', profile_path(current_user), class: "back-to-profile btn btn-success d-flex justify-content-center align-items-center" %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
